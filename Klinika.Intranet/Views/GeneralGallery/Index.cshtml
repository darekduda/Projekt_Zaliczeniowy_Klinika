@model IEnumerable<Klinika.Data.Data.CMS.GeneralGallery>

@{
    ViewData["Title"] = "Index";
}

<link rel="stylesheet" href="https://cdn.datatables.net/1.12.1/css/jquery.dataTables.min.css" />
<script type="text/javascript" src="https://code.jquery.com/jquery-3.5.1.js"></script>
<script type="text/javascript" src="https://cdn.datatables.net/1.12.1/js/jquery.dataTables.min.js"></script>

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nazwa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.imgPath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CzyAktywny)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.imgPath)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CzyAktywny)
            </td>
            <td>
                    <a class="edit" title="Edit" asp-action="Edit" asp-route-id="@item.IdGeneralGallery"><i class="material-icons" style="color: #FFC107">&#xE254;</i></a>
                    <a class="view" title="View" asp-action="Details" asp-route-id="@item.IdGeneralGallery"><i class="material-icons" style="color: #03A9F4">&#xE417;</i></a>
                    <a class="delete" title="Delete" asp-action="Delete" asp-route-id="@item.IdGeneralGallery"><i class="material-icons" style="color: #E34724">&#xE872;</i></a>
@*                <a asp-action="Edit" asp-route-id="@item.IdGeneralGallery">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdGeneralGallery">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdGeneralGallery">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>

<script type="text/javascript">
    $('#example').DataTable({
    });
</script>